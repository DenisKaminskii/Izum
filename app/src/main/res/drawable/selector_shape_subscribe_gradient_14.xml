<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Состояние, когда включено (enabled) -->
    <item android:state_enabled="true">
        <shape android:shape="rectangle">
            <gradient android:angle="45" android:endColor="@color/subscribe_gradient_end" android:startColor="@color/subscribe_gradient_start" />
            <corners android:radius="14dp" />
        </shape>
    </item>

    <!-- Состояние, когда выключено (disabled) -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/white_25" />
            <corners android:radius="14dp" />
        </shape>
    </item>

</selector>